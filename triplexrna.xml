<tool id="triplexrna" name="TriplexRNA" version="0.1.0">
    <requirements>
    </requirements>

    <!-- command -->
    <command detect_errors="exit_code"><![CDATA[
        python '$__tool_directory__/triplexrna' -o '$organism' -g '$identifier' > '$output'
    ]]></command>

    <!-- inputs -->
    <inputs>
        <param name="organism" type="select" label="organism" help="Select the target organism">
            <option value="Human" selected="true">Human</option>
            <option value="Mouse">Mouse</option>
        </param>
        <param name="identifier" type="text" value="" label="identifier" />
    </inputs>

    <!-- outputs -->
    <outputs>
        <data name="output" format="JSON" />
    </outputs>

    <!-- tests -->
    <tests>
        <test>
            <param name="organism" value="Human" />
            <param name="identifier" value="CDKN1A" />
            <output name="output" />
        </test>
    </tests>
    <!-- help -->
    <help><![CDATA[
Usage:  triplexrna -o <organism> [options]

Options:        -o ORGANISM     search all RNA triplexes of the "Human" or "Mouse" model ORGANISM
                -g GENE         retrieve all RNA triplexes involving gene GENE, or a list of genes (comma separated list, or file path with one identifier per line)
                -t TRIPLEX      retrieve all details of RNA triplex TRIPLEX
                -p PATHWAY      retrieve all RNA triplexes of KEGG pathway PATHWAY
                -m MIRNA        retrieve all RNA triplexes involving the miRNA or miRNA-pair MIRNA (comma separated list)
                -x TARGET       retrieve all RNA triplexes of a given miRNA pair (implies -m), involving a gene TARGET or a list of gene targets (comma separated list, or file path with one identifier per line)
    ]]></help>

    <!-- citations -->
    <citations>
        <citation type="bibtex">
@misc{renameTODO,
  author = {LastTODO, FirstTODO},
  year = {TODO},
  title = {TODO},
  url = {https://triplexrna.org},
}</citation>
    </citations>

</tool>
